@startuml
skinparam shadowing false
skinparam swimlaneWidth 220

title Activity Diagram — เข้าสู่ระบบ ลงทะเบียน และโหลดเซสชัน

|ผู้ใช้|
start
:"เปิดเว็บแอป (เข้าหน้าแรกของระบบ)";

|เว็บแอป|
if ("มีโทเค็นที่จัดเก็บในเบราว์เซอร์แล้ว?") then (ใช่)
  :"ส่ง GET /auth/me พร้อม Bearer token";
  |API|
  :"ตรวจสอบโทเค็นและโหลดผู้ใช้";
  |ฐานข้อมูล|
  :"อ่านข้อมูลผู้ใช้";
  |เว็บแอป|
  if ("โหลดโปรไฟล์สำเร็จ?") then (ใช่)
    :"เข้าสู่โหมดใช้งาน (PrivateRoute)";
    stop
  else (ล้มเหลว — โทเค็นไม่ถูกต้อง/หมดอายุ)
    :"ล้างสถานะล็อกอินที่ฝั่งไคลเอนต์";
  endif
else (ไม่มีโทเค็น)
endif

|เว็บแอป|
:"แสดงหน้าเข้าสู่ระบบ";

|ผู้ใช้|
if ("ต้องการลงทะเบียนบัญชีใหม่?") then (ใช่)
  :"กรอกข้อมูลลงทะเบียน";

  |เว็บแอป|
  :"ส่ง POST /auth/register";

  |API|
  :"ตรวจสอบความซ้ำและสร้างผู้ใช้";

  |ฐานข้อมูล|
  :"บันทึกบัญชีผู้ใช้";

  |เว็บแอป|
  :"แจ้งผลการลงทะเบียน / กลับไปกรอกเข้าสู่ระบบ";
else (ไม่ — ใช้บัญชีเดิม)
endif

|ผู้ใช้|
:"กรอกอีเมล (หรือ username) และรหัสผ่าน\nแล้วยืนยันการเข้าสู่ระบบ";

|เว็บแอป|
:"ส่ง POST /auth/login";

|API|
:"ตรวจสอบรหัสผ่านและสร้าง JWT";

|ฐานข้อมูล|
:"อ่านข้อมูลสำหรับตรวจสอบสิทธิ์";

|เว็บแอป|
:"เก็บ access_token";

:"ส่ง GET /auth/me";

|API|
:"คืนข้อมูลโปรไฟล์ผู้ใช้";

|ผู้ใช้|
:"เข้าสู่ระบบสำเร็จ — พร้อมใช้งานส่วนที่อยู่ในขอบเขตของเล่ม";

stop

@enduml
